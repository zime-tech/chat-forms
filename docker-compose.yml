services:
  postgres:
    image: postgres:16-alpine
    container_name: chat-forms-db
    restart: unless-stopped
    environment:
      POSTGRES_DB: chat_forms
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
